<!DOCTYPE html>
<html>
    <head>
        <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <title>Translator</title>
    </head>
    <body>
    {% block body %}
        <div class="container">
            <div class="row">
                <div class="col-sm-12">
                    <h1 class="jumbotron">Welcome to the Translator</h1>
                    <form action="/search" method="get" class="form-group">
                      <input type="text" name="search" list="search"  class="form-control" placeholder="Search" required>
                        <datalist id="search">
                            {% for word in words %}
                            <option value={{word.getWord}}>
                            {% endfor %}
                        </datalist>
                        <br>
                        <div class="center">
                            <button type="submit" class="btn btn-sm">Search</button>
                        </div>
                    </form>
                    <a href="/complete_list"><button type="button">view entire dictionary</button></a>
                </div>
            </div>
        </div>
    {% endblock %}
    </body>
</html>
